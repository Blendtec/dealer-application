import { ISocialChannel } from './social-channel.interface';

export interface ISocial {
  facebook: ISocialChannel;
  pinterest: ISocialChannel;
  instagram: ISocialChannel;
  twitter: ISocialChannel;
  googlePlus: ISocialChannel;
  blog: ISocialChannel;
}
